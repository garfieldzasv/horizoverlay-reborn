# 开发记录

这是 Horizoverlay Reborn 的改造过程记录：每处改动的**原因**、踩过的坑、以及为什么选了这个做法而不是那个。

功能说明和使用方法见 [README.md](README.md)。

---

`https://overlays.ffcafe.cn/horizoverlay/` 的本地化副本，用于自行调整字号 / 布局后直接给 ACT OverlayPlugin 使用。

起因是一次版本更新之后，线上那份的字体开始错位、职业图标整片缺失，而原始仓库早已长期无人维护，
等不到修。于是按自己的用法把这些问题逐个改掉；本来只是自用的调整，完成后顺手推上来发布。

## 目录

| 路径                       | 说明                                                                 |
| -------------------------- | -------------------------------------------------------------------- |
| `site/`                  | 从远端原样下载的产物（含 sourcemap），作为参照，不要改。**不入库**，需要时自行下载 |
| `src/`                   | 从 sourcemap 还原出的完整源码（React 16 + react-router-dom 5）       |
| `src/images/`            | 58 个职业 / 宠物图标，从 bundle 里的 data URI 还原                   |
| `src/testing/testing.js` | 本地模拟数据源，见下                                                 |
| `src/skewAlign.js`       | 运行时实测斜边对齐所需的偏移，喂给横幅和占比条                       |
| `src/configStore.js`     | localStorage 的容错包装，file:// 下取不到时退回内存                  |
| `public/`                | CRA 模板                                                             |
| `preview/`               | 免构建预览：原版 bundle + 强制注入模拟数据，用来对比"改之前长什么样"。**不入库** |

## 本地开发

```bash
npm install --registry=https://registry.npmmirror.com
npm start          # http://localhost:3000/?mock=1#/
```

直连 `registry.npmjs.org` 会 `ECONNRESET`，用上面的镜像（38 秒装完 1323 个包）。

模拟数据只在 URL 带 `mock` 参数时启用，所以构建产物给 ACT 用时不受影响。

| 参数                  | 默认         | 说明                                                                                                                                          |
| --------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `mock=1`            | —           | 启用模拟数据                                                                                                                                  |
| `mockInterval=1000` | 1000         | 刷新周期（毫秒），`0` = 只推一次，截图时用                                                                                                  |
| `mockParty=8`       | 8            | 队伍人数，1–24                                                                                                                               |
| `mockStress=1`      | 关           | DPS/HPS ×10 压到 6 位数，用来验证布局的最宽情况                                                                                              |
| `mockBackdrop=1`    | 跟随`mock` | 深色预览背景（悬浮窗本身透明，浏览器里是白底白字）。单独用可以预览配置模式 —— 配置模式只在**没有数据**时出现，所以不能同时开 `mock` |

例：

- 实际战斗效果：`http://localhost:3000/?mock=1&mockParty=24&mockInterval=0#/`
- 配置模式效果：`http://localhost:3000/?mockBackdrop=1#/`（需在配置页勾上「配置模式」）

## 接入 ACT

```bash
npm run build      # 产物在 build/，7.0MB，自包含
```

在 OverlayPlugin 里新建一个 **MiniParse** 类型的悬浮窗，URL 填：

```
file:///<build 目录的绝对路径>/index.html
```

事件契约和原版完全一致（`onOverlayDataUpdate`），所以原来指向 `overlays.ffcafe.cn` 的配置
直接改 URL 就行。`homepage: "."`，产物用相对路径，放哪个目录都能跑。

- 悬浮窗上**右键**打开配置页（需要 ACT 里的「滑鼠穿透 / Enable clickthru」处于关闭状态）
- 8 人一行需要窗口宽 **≥1517px**；不够会自动折行，不会裁掉卡片
- 也支持 WebSocket 模式：URL 后加 `?HOST_PORT=ws://127.0.0.1:10501/`

### 右键呼不出配置页

产物里修了三个会导致这个现象的 bug（都只在 `file://` 下暴露，远端 https 版不会）：

1. `openConfig` 打开的是 `'./#/config'`，在 `file://` 下解析成**目录**而不是页面
   （`file:///.../build/#/config`）。
2. `window.open` 在 CEF 里通常被拦，返回 `null`，而旧代码直接 `.focus()` —— TypeError
   把整个处理函数打断。现在会回退到**在当前窗口**打开配置页。
3. `index.js` 的 `Root` 里 `<Route path="/">` 没写 `exact`，会把 `/config` 一起吃掉，
   `Switch` 永远渲染 Overlay —— 所以即使导航过去也打不开。

回退到当前窗口时，配置页左侧会多出一个**「返回悬浮窗」**标签用来回去。

**如果右键仍然没反应**，说明事件根本没到页面，和上面无关，按顺序排查：

- ACT 里该悬浮窗的**「滑鼠穿透 / Enable clickthru」必须关闭**，否则鼠标事件直接穿过去
- 直接把 URL 改成下面这个，能打开就说明只是右键没传进来：
  ```
  file:///<build 目录的绝对路径>/index.html#/config
  ```
- 也可以在 OverlayPlugin 里单独再建一个悬浮窗指向上面这个 URL，专门用来改配置

### 如果配置不保存

说明 `localStorage` 在 `file://` 下被 CEF 拦了。悬浮窗仍会正常显示（`src/configStore.js`
会退回内存存储，并在 devtools 控制台留一条 warning），但重启后设置会丢。
改成用本地 HTTP 服务指向 `build/` 即可解决。

### 实测时值得确认的几件事

本地验证全部是模拟数据 + headless Chrome 跑的，下面几项只有真实环境能确认：

1. **字体是否加载** —— 数字应该等宽、`0` 中间有一个点。不对就是 CEF 没吃 woff2。
2. **6 位数** —— 确认真实战斗里 DPS/HPS 会不会上 6 位，卡片有没有挤到。
3. **`Encounter.healed` 的含义** —— HPS 占比条拿它做分母。如果比例明显不对（全是 0 或
   全接近 100%），把 devtools 里某次 `Encounter` 对象的内容发来，按实际字段改。
4. **职业图标** —— 真实 `Combatant.Job` 的大小写若与预期不符，图标会回退成 `error.png`。
   新加的驯兽师（`bst`）图标是从官方素材转换的，也在这一类里。
5. **横幅斜边对齐** —— 底排卡片数为奇数时对不齐，这是几何决定的（见「一个前提」）。
6. **CSS 自定义属性 / `ch` 单位** —— 老版本 OverlayPlugin 的 CEF 若过旧可能不支持，
   表现是卡片宽度塌成 0 或字号异常。主题系统现在完全依赖自定义属性，这条比之前更关键。

## 免构建预览（原版效果）

```bash
node <任意静态服务器> preview/
# 打开 http://127.0.0.1:8099/index.html?mockInterval=0#/
```

`preview/` 是把原版 bundle 的 `publicPath` 改成相对路径、并强制注入模拟数据的副本，
不需要 `npm install`。

## 仓库迁移

2026-09-22 由 `bsides/horizoverlay` 的 fork 迁为独立仓库，旧仓库已归档。
提交历史、tag、release 与 Pages 地址均未变动。

## 已修复

### 配置模式缺整条底栏

原版 `SetupMode.js` 根本没有引入 `Encounter` 组件，所以配置模式下看不到
「区域名 / 总DPS / LB / 战斗时长」那条底栏，而实际使用时 `Overlay.js` 是会渲染它的。
连带后果是配置页里的「战斗时间」「总DPS」「Discord」三个开关在配置模式下**完全没有反馈**。

改法是让 SetupMode 复用真正的 `Encounter` 组件（而不是复制一份 markup），
位置和 `Overlay.js` 里一致 —— 放在 `.combatants` 之后，这样两边不会再次跑偏。
配套加了 `helpers.js` 的 `mockEncounter` 和 `locale.js` 里 5 种语言的 `sampleZone`。

> 注意：悬浮窗**从来不显示**每个角色的死亡次数，也不显示总伤害（底栏只有总 DPS）。
> `deaths` / `crithit%` / `DirectHitPct` / `healed%` 这些 ACT 都推过来了，
> 但只被 `Encounter.sendToDiscord` 用于拼 Discord 消息，没有任何界面渲染。

### 占比横条：DPS + HPS，去掉数字

原版只有一条伤害占比横条，外加一行百分比数字，两者由同一个 `showDamagePercent` 控制。
现在是两条横条、两个开关，百分比数字去掉了：

| config 键         | 配置页标签          | 内容                            |
| ----------------- | ------------------- | ------------------------------- |
| `showDamageBar` | DPS占比条 / DPS Bar | 本人伤害 ÷`Encounter.damage` |
| `showHealBar`   | HPS占比条 / HPS Bar | 本人治疗 ÷`Encounter.healed` |

两条用**同一套 CSS 类**，所以样式完全一致；HPS 条排在 DPS 条下方，相邻的两条之间留 1px。

`Encounter.healed` 在开打前可能是 0 或缺失，除法做了保护，取不到就是 `0%`。

实测（配置模式，8 张卡）填充比例与模拟数据完全吻合：

| 卡片               | DPS 条          | HPS 条 |
| ------------------ | --------------- | ------ |
| 黑魔 / 龙骑 / 忍者 | 19% / 18% / 17% | 0%     |
| 暗黑骑士           | 9%              | 2%     |
| 占星               | 5%              | 49%    |
| 白魔               | 5%              | 47%    |

> 顺带清掉了 `--text-micro`（原来只有百分比数字用它）、`.damage-percent` 的文字样式，
> 以及为保持高度而加的 `.invisible` 占位 —— 现在没有会消失的文字行了。
> 卡片变矮不影响横幅对齐，`skewAlign.js` 会重测。

### 斜边对齐：横幅 + 两条占比条

整个悬浮窗的元素都是 `skew(-30deg)` 的平行四边形，眼睛会把那些斜边读成一条条往下延伸的直线。
所以**任何排在主色带下方的东西，都得按 `tan(30°) × 垂直距离` 向左移**，否则它的边就落在那条线旁边而不是线上。

需要这样处理的有三处，全部由 [`src/skewAlign.js`](src/skewAlign.js) 在每次渲染后实测写入：

| 变量              | 用于                                 | 当前值         |
| ----------------- | ------------------------------------ | -------------- |
| `--bar-shift-1` | DPS 占比条                           | 7.92px         |
| `--bar-shift-2` | HPS 占比条                           | 9.65px         |
| `--band-gap-y`  | 总览横幅（CSS 再由它推出宽度和偏移） | 见「总览横幅」 |

CSS 里的值只是首帧兜底。字号、可选行开关、折行都会改变这些距离，实测因此比写死常量可靠。

实测误差（像素级验证，非 DOM 读数）：占比条 **-0.34px / +0.39px**，横幅左右均 **0.00px**。
两条占比条的符号相反且都在半像素内，是边缘检测的抗锯齿噪声。

> `.damage-percent-bg` 原来是写死的 `left: -8px`，只在某一种布局下碰巧对。
> 另外 `.damage-percent-fg` 在已经倾斜的轨道内部**又叠了一次** `skew(-30deg)`，
> 导致填充前缘是 -60° 而不是 -30°，和其他所有边都不平行 —— 去掉了它自己那层。

> `.row .maxhit` 的 `margin-top` 原本是 `-0.3em`，那是为旧的占比数字行调的（那行自己还
> `top: -2px` 往上压）。数字去掉、又多一条横条之后它就贴死在条子上了，实测间距 0px。
> 改成 `0.3em` 后是 6px。

### rem 复合的字号

原版写的是 `html, body { font-size: 0.9rem }`，`0.9` 被**应用了两次** —— 先把根字号变成
`0.9 × 16 = 14.4px`，body 的 `0.9rem` 再按这个已经缩过的根字号算成 `0.9 × 14.4 = 12.96px`。
读起来像 14.4px，实际渲染 12.96px。

现在根字号回到浏览器默认的 16px，body 单独取 `--text-base: 0.81rem`，也就是同一个 12.96px，
但只写一次 —— 于是 CSS 里其他地方的 `rem` 终于和它看起来的意思一致。

**渲染尺寸刻意保持不变**：配置模式截图改前改后逐字节一致，`1ch` = 7.781px、卡宽 177.63px、
卡距 189.63px、横幅宽 367.25px、dY = 61.14px 全部不变，斜边误差仍为 0.00px。

唯一的变化在配置窗口：那里的 `padding: 0.5rem 0.75rem` 现在按 16px 解析（8/12px，原来是
7.2/10.8px），整页下移约 2px。悬浮窗本体不受影响。

### 字号成体系

原版三个字号里有两个是写死的 px，正文一改就脱节。现在全部挂到 `--text-base` 上：

| 变量             | 值                       | 当前 px | 用在                           |
| ---------------- | ------------------------ | ------- | ------------------------------ |
| `--text-base`  | `0.81rem`              | 12.96   | 正文：名字、DPS/HPS 数值、横幅 |
| `--text-small` | `0.85em`               | 11.02   | 角色名、最强伤害               |
| `--text-label` | `0.6172839506172839em` | 8.00    | ` HPS` / ` DPS` 后缀       |

角色名和最强伤害用同一档（`--text-small`）—— 夹着色带的两行文字读作同一层级，而不是三层。
角色名另有 `padding-bottom: 0.35em` 把它和色带拉开，文字底到色带顶约 3.1px → **5.36px**。

> 这条规则的选择器必须是 `.row .name` 而不是 `.name`：同一个文件里的 `.row > div`
> （1 个类 + 1 个元素）特异性高于单个类，会把 `padding-bottom` 压回 1px。

**观感刻意一点没变**：配置模式截图与改前逐字节一致（md5 相同）。

改 `--text-base` 现在会整体等比缩放 —— 字号、卡片宽度（`ch` 单位）、横幅宽度、横幅对齐
（`skewAlign.js` 重测）全部自动跟上。

> 那两个长小数是 50/81 和 175/324，**故意写全**的：写成 `calc(50em / 81)` 会让字形位移到
> 改变光栅化（实测差 1026 像素），四舍五入到 6 位也会（差 95 像素）。写满才能逐字节一致。

顺带修了 `skewAlign.js` 的一个问题：卡片折行时横幅在**最后一行**下面，原来量的却是第一行。
现在取最靠下那一行的卡片。实测 900px 宽折成 2×4 时，左右误差仍为 0.00px。

### 配置页重做

原版是横向 fieldset 布局，样式用的还是裸元素选择器（`fieldset`、`input[type=checkbox]`、
`legend`），而 CSS 和悬浮窗打包在一起 —— 那些规则一直在影响悬浮窗。现在整页重写：

- 全部样式收进 `.config` 作用域
- 分 section，每个选项一行（标签左、控件右）
- section 用 **多列**排版而不是 grid：各 section 高度差很大，grid 的行高等于该行最高的格子，
  会留大片空白。窗口宽时自动分列，窄到 520px 以下退成单列
- `.config` 自己处理滚动（`height: 100vh; overflow-y: auto`），因为 `html/body` 为了悬浮窗
  设了 `overflow: hidden`
- 弹窗默认尺寸 `500 × 900`（竖排单列）。这个值从 config 里移出来改成模块常量 —— 原来存在
  config 里，而合并是浅合并，存过的旧值会永久遮蔽新默认值
- 设置页自身不再跟随 `zoom` 缩放，否则拖滑杆时整页会在指针下抖动

### 右键

右键打开配置页，所以浏览器原生菜单在 document 级被屏蔽 —— 但**放行 `.config` 内部**，
配置页需要右键粘贴 webhook URL。只在 `.damage-meter` 上挡不住：那个元素只有内容高度，
卡片下方的空白区占了窗口大部分面积。

### 黑白主题

三处修正：

1. **配置写入的是 `blackWhite`，CSS 里写的却是 `.default`** —— 那条规则从来没匹配上过，
   两种模式下都没生效。
2. **Overlay 的根元素不带主题类**（SetupMode 的带），所以任何以主题类为前缀的规则
   都只能在配置模式生效。现在 `.damage-meter` 也带上 `config.color` 了。
3. setupMode.css 里重复了一份 overlay.css 已有的 `.row.self` 规则，删掉。

那条失效的 `.default .row .dps` 我**没有复活**：试着让它生效后，它会在斜切的色带里
画出直角矩形块，破坏造型。所以是删掉而不是修好。

改用它做了一件真正有用的事 —— 黑白模式下伤害条原本是黑底黑填充：

|              | 条填充 vs 背景对比 |
| ------------ | ------------------ |
| byRole       | 22/255             |
| 黑白（改前） | **16/255**   |
| 黑白（改后） | **29/255**   |

改成了白色系（`rgba(255,255,255,0.15)` 轨 / `0.7` 填充），仍是单色，但看得清了。
这是本次唯一的观感改动，只影响黑白主题；byRole 主题截图逐字节一致。

> 黑白主题的主要效果本来就来自 `CombatantHorizontal` —— 它在该模式下不给行加
> `job-*` 类，颜色自然就掉了。这部分原本就是正常的。

## 颜色主题

主题就是一组 CSS 自定义属性。所有上色规则在 `src/css/overlay.css` 里**只写一次**，全部走
`rgba(var(--role), var(--alpha))`，所以加主题不需要按职业再写一遍规则。

| 主题     | class                             | 说明                           |
| -------- | --------------------------------- | ------------------------------ |
| 职业特有 | `byRole`（即 `:root` 默认值） | 防卫蓝 / 治疗绿 / 输出红       |
| 黑白色调 | `.blackWhite`                   | 所有职业色收敛为黑，自己仍是白 |
| 细分职业 | `.byJob`                        | 五职能各一色                   |

### 加一个新主题

```js
// 1. src/helpers.js
export const themes = [ …, { value: 'myTheme', label: 'themeOption4' } ]
```

```css
/* 2. src/css/overlay.css 末尾，只写与默认值不同的部分 */
.myTheme { --role-tank: …; --role-melee: …; }
```

```js
// 3. src/locale.js — 5 个语言各加一个 themeOption4
```

配置页的下拉框由 `themes` 驱动，不用动。

### 可用变量

| 变量                                                                   | 默认              | 作用                                                  |
| ---------------------------------------------------------------------- | ----------------- | ----------------------------------------------------- |
| `--role-tank` / `-healer` / `-melee` / `-ranged` / `-caster` | 见`index.css`   | 五职能的`r, g, b`（裸通道值，便于各处套不同 alpha） |
| `--role-other`                                                       | `0, 0, 0`       | 宠物和无职能单位                                      |
| `--role-self`                                                        | `255, 255, 255` | 自己                                                  |
| `--band-alpha`                                                       | `0.5`           | 色带底色                                              |
| `--band-hi-alpha` / `--band-dim-alpha`                             | `0.5` / `0.1` | 开「高亮色块」后色带被分成的两半                      |
| `--track-alpha` / `--fill-alpha`                                   | `0.3` / `0.7` | 占比条的轨道和填充                                    |

### 配色时注意亮度，不要只看色阶

职业特有那三色恰好是 Material 500（Red / Light Green / Blue），但**同一档色阶不是等亮度的**。
细分职业最初用 Amber 500 做远敏，合成后相对亮度 0.34，而其余四色在 0.14–0.25 —— 结果白色数字
压在上面只有 2.7:1 对比，高亮的明暗分界也被冲淡。改成较暗的芥末金 `205,160,45`（亮度 0.24，
与绿色齐平）后，白字对比回到 3.79:1。

### 职能划分

`jobRoles` 把职业分成五类，宠物跟随主人：学者小仙女归治疗，召唤宠归法系，机工炮台归远敏。
**主题完全不参与 JS** —— `job-*` 类名无条件输出，用不用由 CSS 决定。

## 卡片几何

几何参数集中在 `src/css/index.css` 的 `:root`，改一处就够：

| 变量               | 值                                | 含义                                                 |
| ------------------ | --------------------------------- | ---------------------------------------------------- |
| `--text-base`    | `0.81rem` = 12.96px             | 正文字号，字号体系与几何都以此为基准                 |
| `--stat-digits`  | `6`                             | DPS/HPS 最宽要装几位数                               |
| `--job-icon`     | `20px`                          | 职业图标尺寸                                         |
| `--icon-slot`    | `--job-icon / 2 + 3px`          | 中间给图标留的空，不留会被 6 位数压住                |
| `--card-width`   | `6*2ch + 7.5ch + 2*--icon-slot` | 卡片宽度                                             |
| `--card-gap`     | `6px`                           | 卡片间距                                             |
| `--band-gap-y`   | 运行时实测                        | 卡片色带与横幅色带的垂直距离，CSS 里的值只是首帧兜底 |
| `--skew-reach`   | `--band-gap-y × 0.5774`        | 斜边在该垂直距离上向左的偏移，tan(30°)              |
| `--banner-width` | `2×卡宽 + 2×间距`             | 横幅宽度，见下                                       |

字体是等宽的，所以 `1ch` 正好是一个数字的宽度，**卡片宽度会跟着字号自动走** —— 调字号不用重算几何。

当前实测（Maple Mono CN，advance 0.6em）：

- `1ch` = **7.78px**，`--card-width` = **177.6px**（原来是写死的 140px），卡片间距 = **189.6px**
- 一行放下 4 / 6 / 8 / 12 / 24 人分别需要约 **759 / 1138 / 1517 / 2276 / 4551px**

放不下时会**折行**，不再像原版那样把首尾的卡片静默裁掉。

## 总览横幅

横幅（区域名 / 总DPS / LB / 战斗时长）的**左右两条斜边都与卡片斜边在同一直线上**。

`skew(-30deg)` 把卡片的**左右边缘都**向左拖了 `--skew-reach = tan(30°) × dY`。所以要让两边都接上，
横幅就是「第 N/2 张卡的左边 到 第 N/2+1 张卡的右边」这一段，整体左移同样的距离：

```
--banner-width = 2×卡宽 + 2×间距 = 2×177.6 + 2×6 = 367.3px
left           = −--skew-reach  = −35.3px
```

实测左右边缘误差均为 **0.03px**。

> **代价是它不再居中。** 横幅中心落在卡片列表中心左侧 35.3px 处 —— 这是几何决定的，
> 不是可调项：两边共线要求中心在 `C − --skew-reach`，与居中（中心在 `C`）直接冲突。
> 想要居中就只能放弃右边那条线。

空间不够时只有区域名会截断，数值永远完整显示 —— 数值才是横幅的意义。

### 一个前提

**底排卡片数必须是偶数。** 推导要求对齐第 N/2 与 N/2+1 张卡的边，N 为奇数时列表中心
落在某张卡的**中间**而不是边上，对不齐。4/6/8 人都没问题，7 人会差半个卡距。

### dY 是实测的，不是写死的

`--band-gap-y` 由 [`src/skewAlign.js`](src/skewAlign.js) 在每次渲染后测量真实布局写入，CSS 里那个
值只是首帧的兜底。卡片高度一变（最强伤害 / 伤害条开关、字号改动、webfont 换上），下一帧就跟上。

写入只影响横幅的水平 `left`，不影响任何高度，所以一轮就收敛，不会自激。实测：

| 配置       | 卡片高 | dY    | 左误差 | 右误差 |
| ---------- | ------ | ----- | ------ | ------ |
| 全开       | 73.56  | 61.14 | 0.00px | 0.00px |
| 关最强伤害 | 57.05  | 44.63 | 0.00px | 0.00px |
| 全关       | 43.55  | 31.13 | 0.00px | 0.00px |

## 已知的显示问题（待改）

暂无。
